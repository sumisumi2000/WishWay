<%# アイコンをお気に入り後のものに切り替え %>
<%= turbo_stream.replace "favorite_#{@wish_list.id}", partial: "favorites/filled_heart_icon", locals: { wish_list: @wish_list } %>
<%# フラッシュメッセージを更新 %>
<%= turbo_stream_flash %>