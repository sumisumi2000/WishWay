<%= turbo_stream.remove "wish_frame_#{@wish.id}" %>
<%# Wishが存在しない場合、説明文を追加 %>
<% if @wishes.blank? %>
  <%= turbo_stream.append 'wishes', partial: 'nothing_wishes' %>
<% end %>
<%# グラフ更新 %>
<%= turbo_stream_graph %>
