<div class="w-5/6 mx-auto max-w-screen-xl">
  <%= form_with model: wish do |form| %>
    <!-- タイトル -->
    <%= form.label :title, class: "label" %>
    <%= form.text_field :title, class: "input input-bordered w-full max-w-xs form-control" %>

    <%= form.submit class: "btn mt-4" %>
  <% end %>
</div>